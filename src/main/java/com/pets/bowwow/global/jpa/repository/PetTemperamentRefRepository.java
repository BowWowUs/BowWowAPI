package com.pets.bowwow.global.jpa.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.pets.bowwow.global.jpa.entity.PetTemperamentRef;

public interface PetTemperamentRefRepository extends JpaRepository<PetTemperamentRef, Long>{

}
